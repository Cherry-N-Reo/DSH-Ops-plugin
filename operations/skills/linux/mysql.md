# Linux MySQL skill

Use for sample Linux staging assets only. Prefer read-only inspection, confirm the asset before maintenance, and record only redacted audit metadata. Never place passwords, tokens, connection strings, or query output in a runbook or audit record.
